const PORT = 3001;

const SALT_ROUND = 10;

const JWT_SECRET = "devTinder@super-secure";

const JWT_EXPIRE = "1d";

const ALLOWED_SKILLS_LEGNTH = 10;

module.exports = {
  PORT,
  SALT_ROUND,
  JWT_SECRET,
  JWT_EXPIRE,
  ALLOWED_SKILLS_LEGNTH,
};
